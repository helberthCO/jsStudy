const secretNumberNode = document.querySelector('.number');
const guessInputNode = document.querySelector('.guess');
const checkBtnNode = document.querySelector('.check');
const againBtnNode = document.querySelector('.again');
const messageNode = document.querySelector('.message');
const scoreNode = document.querySelector('.score');
const appWrapperNode = document.querySelector('.app__wrapper');
const highScoreNode = document.querySelector('.highscore');

export {
  secretNumberNode, 
  guessInputNode, 
  checkBtnNode, 
  againBtnNode, 
  messageNode, 
  scoreNode, 
  appWrapperNode,
  highScoreNode
};